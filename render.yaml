services:
  - type: web
    name: helmet-detection-system
    env: python
    region: singapore
    plan: free
    buildCommand: "pip install -r requirements_render.txt"
    startCommand: "gunicorn app_upload:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
